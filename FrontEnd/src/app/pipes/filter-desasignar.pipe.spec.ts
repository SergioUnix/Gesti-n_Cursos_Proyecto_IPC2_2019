import { FilterDesasignarPipe } from './filter-desasignar.pipe';

describe('FilterDesasignarPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDesasignarPipe();
    expect(pipe).toBeTruthy();
  });
});
